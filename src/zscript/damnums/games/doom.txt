/*
 * DamNums: by Xaser Acheron
 *
 * Doom monsters.
 */

class DamCacodemon : Cacodemon replaces Cacodemon
{
	// [XA] will likely need to add this shim for all actors, ohno :<
	override int TakeSpecialDamage(Actor inflictor, Actor source, int damage, Name damagetype) { return DamNum.create(self, inflictor, damage, damagetype); }
}
